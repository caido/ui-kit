import {defineComponent} from "./utils";

export default defineComponent({
  passthrough: {
    column: {
      headerCell: {
        style: {
          borderBottomWidth: "0.25rem",
        },
      },
    },
  }
});
