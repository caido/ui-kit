---
index: true
---

# Get started

Everything here rests on one idea: a value is settled once and read everywhere, so a colour, a gap or a type size is chosen from a name rather than decided again on each screen.

<GetStartedIndex />
